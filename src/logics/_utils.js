import { Navigation } from 'react-native-navigation';
import { resetToken, accountLogout } from '../actions/action.account';

export const checkSession = (access_token, dispatch, nextAction) => {
    
};

export const anotherFunction = () => {

};
